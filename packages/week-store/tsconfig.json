{
  "extends": "../../tsconfig.json",
  "include": [
    "src" // Включает все файлы в папке src и её подкаталогах
  ],
  "exclude": [
    "node_modules", // Исключает папку node_modules
    "dist" // Исключает папку dist
  ]
}